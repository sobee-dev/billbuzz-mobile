// // components/SubscriptionLockedBanner.tsx — owner sees a URL to type, no tappable link


// {user?.role === 'owner' ? (
//   <Text>Your subscription needs attention. Visit billing.billbuzz.app to manage it.</Text>
// ) : (
//   <Text>This business's subscription is inactive. Contact your business owner.</Text>
// )}